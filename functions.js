const sayHello = () => console.log("Hello World 2");

const add = (a,b) => { return (a+b) }

module.exports = {
    sayHello,
    add

    //jeśli nazwy są inne od argumentów, należy je tu wpisać po dwukropku. 
}